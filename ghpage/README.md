# NasBeat GitHub Page

A small, fast SvelteKit site used to host the NasBeat landing page on GitHub Pages.

---

## Quick start

```bash
cd ghpage
npm install
npm run dev      # dev server
npm run build    # produces ghpage/build
```

## Deploy

Push to the `main` branch. The CI workflow automatically builds and publishes to GitHub Pages.

---
